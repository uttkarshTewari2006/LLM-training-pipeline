seed: 42
dataset: cifar10
data_dir: data
num_classes: 10

training:
  epochs: 5
  batch_size: 128
  num_workers: 2
  learning_rate: 0.001
  weight_decay: 0.0001
  checkpoint_dir: checkpoints

model:
  name: resnet18
  pretrained: false
